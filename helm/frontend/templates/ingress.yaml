{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ecommerce
  namespace: {{ .Release.Namespace }}
  annotations:
    kubernetes.io/ingress.class: gce
    kubernetes.io/ingress.global-static-ip-name: {{ .Values.ingress.staticIpName }}
    kubernetes.io/ingress.allow-http: {{ .Values.ingress.allowHttp | quote }}
    {{- if .Values.ingress.managedCertificate.enabled }}
    networking.gke.io/managed-certificates: {{ .Values.ingress.managedCertificate.name }}
    {{- end }}
    {{- if .Values.ingress.frontendConfig.enabled }}
    networking.gke.io/v1beta1.FrontendConfig: {{ .Values.ingress.frontendConfig.name }}
    {{- end }}
spec:
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          - path: /products
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ingress.productServiceName }}
                port:
                  name: {{ .Values.ingress.productServicePortName }}
          - path: /orders
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ingress.orderServiceName }}
                port:
                  name: {{ .Values.ingress.orderServicePortName }}
          - path: /cart
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ingress.cartPaymentServiceName }}
                port:
                  name: {{ .Values.ingress.cartPaymentServicePortName }}
          - path: /checkout
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ingress.cartPaymentServiceName }}
                port:
                  name: {{ .Values.ingress.cartPaymentServicePortName }}
          - path: /payments
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ingress.cartPaymentServiceName }}
                port:
                  name: {{ .Values.ingress.cartPaymentServicePortName }}
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "frontend.fullname" . }}
                port:
                  name: http
{{- end }}
