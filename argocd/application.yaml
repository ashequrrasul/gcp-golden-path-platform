apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: golden-path-microservice
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/ashequrrasul/gcp-golden-path-deploy.git
    targetRevision: main
    path: helm/microservice
    helm:
      releaseName: golden-path-microservice
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: golden-path
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
